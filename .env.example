# This file documents configuration names only.
# Never place real credentials or secrets in this file.

APP_ENV=local
LOG_LEVEL=INFO
AWS_REGION=us-east-1
NASA_FIRMS_MAP_KEY=
EO_POSTGRES_PASSWORD=
EO_POSTGRES_HOST_PORT=55432

# Add Kafka, Spark, and AWS configuration variables
# only when their implementation milestone begins.
